@model IEnumerable<kundt_back_end.Models.tblEyecatcher>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GUIDBild)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Eyecatcher)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GUIDBild)
        </td>
        <td>
            @*@if (item.Eyecatcher == null)
            {
                item.Eyecatcher = new byte
            }*@

            @{ 
                var image = Convert.ToBase64String(item.Eyecatcher);
            }
            @Html.Raw("<img src=\"data:"+ item.Eyecatcher +";base64," + image + "\"/>")
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDEyecatcher }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDEyecatcher }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDEyecatcher })
        </td>
    </tr>
}

</table>
